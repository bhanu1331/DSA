class Solution {
    public boolean isArmstrong(int n) {
        double ans=0;
        int temp=n;

        while(temp>0){
            int r=temp%10;
            ans=ans+Math.pow(r,3);
            temp=temp/10;
        }
        return (ans==n);

    }
}
